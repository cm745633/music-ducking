# 診斷版：雜音是從哪一段來的

你聽到的雜音，可能來自四個地方：合成引擎的原始輸出、mp3 編碼、我加的響度處理、
或是耳機。**這個版本的目的是把這四個變因拆開，一個一個排除。**

它是一個獨立的 App（名字是「旁白插播 · 診斷」），**可以跟正式版同時裝在手機上**，
資料也分開，不會互相影響。

---

## 一、它跟正式版的差別

| | 正式版 | 診斷版 |
|---|---|---|
| 句數 | 600 條 | **60 條**（6 角色 × 10） |
| 音訊 | 處理過的 64k mp3 | **同一句有四個版本可切換** |
| 播放端 | 0 dB 以上會啟用系統增幅 | **只做衰減，完全不加工** |

只有 60 條，是因為未壓縮的 wav 每條約 350 KB，600 條就是 204 MB，
遠超過 GitHub 網頁上傳 25 MB 的單檔上限。這 60 條是刻意挑的：
每個角色取 5 條「處理得最重的」（提升幅度最大、壓縮器和限制器動得最兇）
加 5 條平均分布的，最容易暴露問題。

---

## 二、四個版本各是什麼

| | 內容 | 這一格跟上一格有差，代表 |
|---|---|---|
| **A** | 原始 WAV，未處理、未編碼 | — |
| **B** | 只做響度正規化 → 128 kbps mp3 | mp3 編碼本身的影響 |
| **C** | 只做響度正規化 → 64 kbps mp3 | 位元率不夠（正式版用的就是 64k） |
| **D** | 正式版：壓縮器＋軟削 → 64 kbps mp3 | 我加的響度處理造成的 |

**四個版本的音量已經自動對齊了**（各自量測過 LUFS，統一衰減到同一個水準）。
這很重要——人耳會把比較大聲的誤判成比較吵、比較清楚。

---

## 三、怎麼測

1. 裝好之後打開 App，**戴上你平常用的耳機**
2. 找個安靜的地方，不要放音樂（有音樂會蓋掉雜音）
3. 按 **循環試播（同一句 A→B→C→D）**
4. 它會連續播四次同一句話，畫面上會顯示現在是哪一個版本
5. 多按幾次，換不同的句子聽

**背靠背聽是關鍵。** 隔一分鐘再比是比不出來的。

---

## 四、怎麼看結果

| 你聽到的 | 結論 | 下一步 |
|---|---|---|
| **A 就有雜音** | 問題在合成引擎的輸出，或在耳機 | 先用電腦喇叭或別的耳機聽同一個 wav 確認。如果電腦上也有，就是引擎參數的問題 |
| A 乾淨，**B 開始有** | mp3 編碼本身的問題 | 很少見。改用 opus 或提高位元率 |
| A、B 乾淨，**C 開始有** | **64 kbps 太低** | 提高位元率。但 96k 會讓檔案超過 GitHub 25 MB 上限，得改成拆成多個檔案上傳 |
| A、B、C 乾淨，**只有 D 有** | **我加的壓縮器／軟削造成的** | 把 v1.1 的處理鏈退回 v1.0 那種只做正規化的版本，音量改用系統增幅補 |
| 四個都乾淨 | 雜音不在這 60 條裡 | 用下面的方法標記正式版裡有問題的句子 |

**最可能的是 C 或 D。** 64 kbps 對 44.1 kHz 的語音本來就偏低，
而 v1.1 為了把音量拉上去加了壓縮器和軟削，會把原本聽不到的底噪一起抬起來。

---

## 五、順便標記

標記系統跟正式版一樣都能用。聽到哪一句特別糟，就用通知的 ✕ 鍵或時間軸標起來，
最後從「設定與匯出」匯出 `flags.csv`。

診斷版的匯出檔會蓋掉正式版匯出的同名檔案，先把正式版的匯出結果收好再測。

---

## 六、告訴我什麼

測完之後，跟我說這幾件事就夠了：

1. **雜音從哪一格開始出現**（A / B / C / D）
2. 換過別的耳機或喇叭聽，結論有沒有變
3. 是所有句子都這樣，還是只有某些角色（特別留意まお，那幾條被提升得最多）

我就能直接改對應的那一段。
