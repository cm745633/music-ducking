# DEV.md — 給下一輪開發者

隨機日文旁白插播器。使用者只裝 APK，不會開 Android Studio。
交付路徑是 GitHub Actions 建置 → 發成 Release → 手機直接下載。

---

## 1. 專案結構

```
narrator-apk/
├─ .github/workflows/build.yml     GitHub Actions：建置 debug APK 並發成 Release
├─ settings.gradle.kts             AGP 8.2.2 / Kotlin 1.9.22 / Gradle 8.5
├─ build.gradle.kts
├─ gradle.properties               含 kapt 在 JDK 17 上需要的 --add-opens
├─ tools/
│  ├─ build_assets.py              從 wav 重建 voices.bin + index.json
│  └─ loudness_report.csv          600 條的響度前後對照
└─ app/src/main/
   ├─ AndroidManifest.xml
   ├─ assets/
   │  ├─ voices.bin                600 段 mp3 首尾相接（18.8 MiB）
   │  └─ index.json                每條的 id / 文本 / offset / length
   ├─ res/                         Material 3 預設，六個 layout，無美術資源
   └─ java/com/narrator/jp/
      ├─ VoiceIndex.kt             assets 索引，記憶體快取
      ├─ Db.kt                     Room：PlayLog / Flag / ClipGain / DAO / AppDb
      ├─ Prefs.kt                  SharedPreferences
      ├─ Reasons.kt                分類代碼
      ├─ Picker.kt                 加權間隔 + ring buffer 選曲
      ├─ VoicePlayer.kt            音訊焦點 + MediaPlayer
      ├─ NarratorService.kt        前景服務、迴圈、通知、標記入口 A
      ├─ ShakeDetector.kt          標記入口 B
      ├─ FlagTileService.kt        標記入口 C（Quick Settings Tile）
      ├─ TimelineActivity.kt       標記入口 D（事後補標）
      ├─ Flagger.kt                標記寫入（刻意獨立於 Service）
      ├─ Buzz.kt                   50ms 震動回饋
      ├─ Exporter.kt               flags.csv / blacklist.txt / stats.md
      ├─ MainActivity.kt           主頁（含試播）
      ├─ ReviewActivity.kt         待分類
      └─ SettingsActivity.kt       設定與匯出
```

---

## 2. 資產格式

### index.json

```json
[
  {
    "id": "kohaku_007",
    "file": "kohaku_007.mp3",
    "character": "コハク",
    "text": "光が葉の重なりの間を細かく漏れる。",
    "domain": "光影",
    "offset": 231045,
    "length": 30288
  }
]
```

`offset` / `length` 是 `voices.bin` 內的位元組區間。

角色與檔名前綴的對應（assets 內不放非 ASCII 檔名）：

| 角色 | 前綴 | domain |
|---|---|---|
| コハク | `kohaku` | 光影 |
| まお | `mao` | 天候 |
| まい | `mai` | 室內 |
| るな | `runa` | 聲音 |
| 中2 | `chuni` | 時間 |
| 花音 | `kanon` | 人物動作 |

### 為什麼打包成單一 blob 而不是 600 個檔案

使用者是用 GitHub 網頁上傳的，**一次 commit 最多 100 個檔案**。
600 個 mp3 要分六批傳，對非開發者是不可接受的失敗點。

代價是 `build.gradle.kts` 裡這段不能拿掉：

```kotlin
androidResources {
    noCompress += "bin"
}
```

aapt 若把 `voices.bin` 壓縮進 APK，`AssetManager.openFd()` 會直接丟
`FileNotFoundException: This file can not be opened as a file descriptor;
it is probably compressed`。播放路徑是：

```kotlin
assets.openFd("voices.bin").use { afd ->
    mp.setDataSource(afd.fileDescriptor, afd.startOffset + clip.offset, clip.length)
}
```

`voices.bin` 目前 18.8 MiB。**GitHub 網頁上傳的單檔上限是 25 MB**，
如果之後語音量增加超過這個數字，就得改成分成 `voices_0.bin` / `voices_1.bin`
之類的多個 blob（index.json 加一個 `blob` 欄位），或改走 git 指令上傳。

---

## 3. 換掉語音、重新出 APK

1. 在 PC 上用 VOICEVOX 相容引擎重新合成，wav 放回原本的
   `<角色>/out_<角色>/<角色>_NNN.wav`
2. 句子有增刪的話，同步改 `synth_<角色>.py` 裡的 `LINES` 陣列
   （**這個陣列就是文本與檔名的唯一對應來源**，index 是從它重建的）
3. 跑：

   ```bash
   python3 tools/build_assets.py --src "/path/to/AivisSpeech Sound" \
                                 --out app/src/main/assets
   ```

4. 把新的 `voices.bin` 與 `index.json` 上傳覆蓋到 GitHub，自動重新建置

`build_assets.py` 需要 `ffmpeg` 在 PATH 上，其餘只用標準函式庫。

---

## 4. 響度處理：為什麼不是一行 loudnorm

原始素材是 44.1 kHz 單聲道 wav，引擎已做過峰值正規化（每條都剛好頂到
0 dBFS），但 **integrated loudness 從 -20.86 到 -10.91 LUFS，落差 9.95 dB**。
這是必須處理的：全域增益在數學上不可能修正條與條之間的落差。

規格書原本寫的是單趟 `loudnorm=I=-16:TP=-1.5:LRA=11`。實作時改成兩段式：

1. `loudnorm ... print_format=json` **只量測**，取 `input_i`
2. `volume=<-16 減去實測值>dB` 線性增益，再接 `alimiter=limit=0.794`（-2 dBFS）收峰值

原因：ffmpeg 4.x 的 loudnorm 對這種 2～6 秒、`input_lra` 為 0 的短句，
第一趟回報的 `target_offset` 會是離譜的值（實測有 +19.39 dB）。
把它照文件餵回第二趟，該條會被反向壓掉將近 20 dB。
**600 條裡有 48 條中招**，輸出響度掉到 -35 LUFS。這個 bug 在單趟模式下
不會出現，但單趟模式的 dynamic 正規化對短句本身就不準。

處理後的實測結果（`tools/loudness_report.csv` 有每條的數字）：

| | 原始 wav | 處理後 mp3 |
|---|---|---|
| 響度全距 | 9.95 dB | **1.91 dB** |
| 標準差 | — | 0.22 dB |
| 最大 true peak | +1.17 dBTP | +0.18 dBTP |
| 總體積 | 208 MB | 18.8 MiB |

### 仍然偏低的 4 條

輸出偏離目標超過 1.5 dB 的只有這四條，全部集中在まお，全部偏低：

| id | 原始 LUFS | 套用增益 | 輸出 LUFS | 偏離 |
|---|---|---|---|---|
| `mao_019` | -18.64 | +2.64 | -18.02 | -2.02 |
| `mao_018` | -19.54 | +3.54 | -17.92 | -1.92 |
| `mao_032` | -17.46 | +1.46 | -17.59 | -1.59 |
| `mao_038` | -19.70 | +3.70 | -17.57 | -1.57 |

（偏離超過 1.0 dB 的有 28 條，超過 1.5 dB 的就是上面這四條。
全部的數字在 `tools/loudness_report.csv`。）

值得注意的是 `mao_032` 只加了 1.46 dB 卻仍偏低 1.59 dB——所以這不單純是
限制器削掉的量，而是 EBU R128 的閘控在這幾條特定短句上的行為。
四條都是まお（天候），推測與該角色語尾偏弱、句子偏短有關。

這正是規格 4.6 第三層「單條增益微調」要救的個案：在待分類頁對這幾條設
+2 dB 即可。若之後要重新合成，在引擎端把這幾句的 `volumeScale` 拉高後重出，
就不需要 App 端補償。

---

## 5. 實作上的決定與偏離規格之處

| 項目 | 規格 | 實作 | 理由 |
|---|---|---|---|
| `Flag` 欄位 | 只有 `logId` | 多存一個 `clipId` | 匯出與排除池不必每次 join；PlayLog 30 天後會清掉，但 Flag 要留 |
| 標記寫入位置 | 未指定 | 獨立的 `Flagger` object | QS Tile 可能在服務沒跑時被點，那時不該（也不能）為了寫一筆標記去啟動前景服務 |
| 試播（4.6.2） | 未指定歸屬 | 在 MainActivity 用獨立的 VoicePlayer，不經過 Service | 服務沒開也能校準；不寫入 PlayLog |
| 時間軸範圍 | 「今天」 | 最近 7 天 | 事後補標常常跨日 |
| 單條增益 UI | -6 ～ +6 dB | 1 dB 一階的清單 | 比滑桿少一半 UI 程式碼，精度足夠 |
| 排除池 | 「標記後排除」 | 排除 `reason IS NULL OR reason NOT IN ('mistake','good')` | `good` 是正面標記，不該被排除 |
| WakeLock | 未提 | 執行期間持有 PARTIAL_WAKE_LOCK | 螢幕關閉時 CPU 若被 suspend，`delay()` 不會準時醒；驗收條件第 1 條要求連續 4 小時不漏播 |
| 音訊 usage | 未指定 | `USAGE_ASSISTANCE_NAVIGATION_GUIDANCE` + `CONTENT_TYPE_SPEECH` | 這正是導航語音用的組合，ducking 的體感最接近目標 |

### 刻意沒做的事

- **不動系統音量。** 全程只用 `AudioFocusRequest(AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK)`
  與 `MediaPlayer.setVolume()`。驗收條件第 2 條（Spotify 音量條不移動）靠這個成立。
- **通知不出聲。** channel 是 `IMPORTANCE_LOW` 且 `setSound(null, null)`、
  `setSilent(true)`、`setOnlyAlertOnce(true)`。標記成功只有 50ms 震動。
- **沒有 release 簽章。** debug keystore 由 Gradle 自動產生，側載安裝正常。
  要上架才需要處理。

---

## 6. 尚未驗證的部分（重要）

**這個專案沒有經過實際編譯。** 產出環境的網路被完全阻擋
（`dl.google.com`、`maven.google.com`、`services.gradle.org`、`repo1.maven.org`
全部回 403），Android SDK 與 Gradle 依賴一個都下載不到。

已經做過的檢查（`tools/` 沒有收錄這支腳本，內容如下）：

- `index.json` 600 筆、offset 連續、總長與 `voices.bin` 位元組數相符
- 從 blob 偏移切出的片段能被 ffprobe 獨立解碼成 mp3（抽查首、中、末三條）
- 14 個 XML 全部可解析
- Kotlin 內所有 `R.id` / `R.layout` / `R.string` / `R.drawable` / `R.color` 引用都對得上實際資源
- XML 之間的資源引用一致
- AndroidManifest 宣告的四個 Activity 與兩個 Service 都有對應的原始檔
- 16 個 Kotlin 檔括號平衡、package 宣告一致
- `noCompress += "bin"`、`buildConfig = true`、`kapt(room-compiler)`、`minSdk = 26` 都在

**這些檢查抓不到型別錯誤、Room 的 DAO 驗證錯誤、依賴版本衝突。**
第一次建置有一定機率會失敗。失敗時 GitHub Actions 的日誌會指出確切檔名與行號，
把紅字貼回來修即可。

風險比較高的幾個點，優先看這裡：

1. **kapt + Room 2.6.1 在 JDK 17 上**。`gradle.properties` 已預先加了
   `--add-opens=jdk.compiler/...` 一整排作為保險。若仍失敗，改用 KSP：
   在 settings 加 `id("com.google.devtools.ksp") version "1.9.22-1.0.17"`，
   把 `kapt(...)` 改成 `ksp(...)`。
2. **AGP 8.2.2 / Gradle 8.5 / Kotlin 1.9.22 的搭配**。這三個版本是配套的，
   要動請整組一起動。
3. **`androidResources { noCompress += "bin" }` 的 DSL 寫法**在 AGP 8.x 才成立，
   舊寫法是 `aaptOptions { noCompress "bin" }`。
4. **`ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK`** 是 API 29 的常數，
   已用 `Build.VERSION.SDK_INT >= Q` 包起來。

---

## 7. 驗收條件對照

| # | 條件 | 狀態 |
|---|---|---|
| 1 | 連續 4 小時不中斷、不漏播 | 實作到位（前景服務 + WakeLock + 電池白名單引導），**需實機驗證** |
| 2 | Spotify 音量條不移動 | 設計上成立（全程未觸碰 `STREAM_MUSIC`），**需實機驗證** |
| 3 | 鎖定畫面展開通知 → 點 ✕ → 震動 → 不需解鎖 | 實作到位（`PendingIntent.getService`，非 Activity trampoline），**需實機驗證** |
| 4 | 標記第 2、3 條對應到正確句子 | 實作到位（slot → `recent[slot].logId` → PlayLog），**需實機驗證** |
| 5 | csv 能被 Excel 正確開啟（UTF-8 BOM） | 實作到位（`sb.append('\uFEFF')`） |
| 6 | 交付實際建置完成並安裝驗證過的 APK | **未達成。** 建置環境無法建立，改交付原始碼 + 自動建置流程 |
| 7 | 音量滑桿即時生效 | 實作到位（每次播放讀 prefs；試播中的那句立即套用） |
| 8 | 試播鈕觸發完整 ducking | 實作到位（與正式播放走同一個 `VoicePlayer.play(duck = true)`） |

第 6 條沒有達成，其餘七條在程式碼層面都已實作，但都還沒有在真機上跑過。
