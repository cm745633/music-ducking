# 怎麼把這個資料夾變成一個能裝在手機上的 App

你不需要懂程式，也不用在電腦上裝任何開發工具。
整件事就是：**把這個資料夾上傳到 GitHub，它會自動幫你做出 APK，然後你用手機下載安裝。**

全程約 15 分鐘，其中 5 分鐘是等機器跑。

---

## 步驟 1：註冊 GitHub 帳號（已有帳號就跳過）

1. 用電腦瀏覽器開 <https://github.com/signup>
2. 依序填 email、密碼、使用者名稱（英文，之後會出現在網址裡）
3. 收信點驗證連結

免費方案就夠用，不用付費，不用綁信用卡。

---

## 步驟 2：建立一個「儲存庫」（repository）

儲存庫就是一個放檔案的資料夾，只是放在 GitHub 上。

1. 登入後，點右上角的 **`+`** → **New repository**
2. **Repository name** 填：`narrator`（或任何你喜歡的英文名字）
3. **Public / Private 選哪個？**

   | | Public（建議） | Private |
   |---|---|---|
   | 手機下載 APK | 直接點連結就能下載 | 手機瀏覽器要先登入 GitHub |
   | 誰看得到內容 | 任何人（包含那 600 條語音） | 只有你 |

   你要的是「手機瀏覽器能直接點連結下載安裝」，**選 Public 最省事**。
   如果不希望語音檔公開，選 Private 也可以，只是手機下載前要先在瀏覽器登入 GitHub。

4. 勾選 **Add a README file**
5. 按 **Create repository**

---

## 步驟 3：打開「寫入權限」（這步很容易漏，漏了會失敗）

GitHub 新建的儲存庫預設不准自動化流程建立下載頁面。要先打開：

1. 在儲存庫頁面上方點 **Settings**（齒輪圖示那排的最右邊）
2. 左側選單找到 **Actions** → 點開後選 **General**
3. 捲到頁面最下面的 **Workflow permissions**
4. 選 **Read and write permissions**
5. 按 **Save**

> 沒做這步的話，App 會build成功但發布失敗，錯誤訊息會寫 `403` 或 `Resource not accessible by integration`。

---

## 步驟 4：讓 Windows 顯示隱藏檔案（這步也很容易漏）

這個資料夾裡有兩個以 `.`（點）開頭的項目：`.github` 和 `.gitignore`。
Windows 預設把它們藏起來，藏起來就不會被上傳，**不上傳 `.github` 的話整件事不會發生**。

- **Windows 11**：檔案總管上方 → **檢視** → **顯示** → 勾選 **隱藏的項目**
- **Windows 10**：檔案總管上方 → **檢視** 索引標籤 → 勾選 **隱藏的項目**

做完之後，你應該會在資料夾裡看到一個半透明的 `.github` 資料夾。

---

## 步驟 5：上傳檔案

1. 回到儲存庫頁面，點 **Add file** → **Upload files**
2. 開啟本資料夾（`narrator-apk`），按 **Ctrl + A** 全選裡面**所有**項目

   要選到的東西（確認 `.github` 有在裡面）：

   ```
   .github        ← 最重要，沒有它就不會自動建置
   .gitignore
   app
   gradle.properties
   build.gradle.kts
   settings.gradle.kts
   tools
   （還有這幾個 .md 說明檔，上傳與否都可以）
   ```

3. 把選好的東西直接拖進網頁中間那塊虛線框
4. 檔案有點多（600 條語音已經打包成一個 19 MB 的檔案），等它跑完進度條
5. 捲到頁面下方，按綠色的 **Commit changes**

---

## 步驟 6：等它自己做好

1. 點儲存庫上方的 **Actions**
2. 你會看到一列「建置 APK」，前面有個轉圈的黃點
3. 泡杯茶。**約 3～6 分鐘**後黃點會變成綠色勾勾

   - 綠勾 → 成功，往下走
   - 紅叉 → 看本文件最後一節

---

## 步驟 7：用手機安裝

建置成功後，你的固定下載網址是（把 `你的帳號` 換成你的 GitHub 使用者名稱）：

```
https://github.com/你的帳號/narrator/releases/latest/download/narrator.apk
```

**這個網址永遠指向最新版本**，之後更新語音重新建置，網址不用換。

用手機做這幾件事：

1. 手機瀏覽器開上面那個網址，會直接開始下載
2. 下載完點開檔案，Android 會問「要允許從這個來源安裝應用程式嗎？」
   → 點 **設定** → 把 **允許來自此來源** 打開 → 按返回 → 再點一次安裝
3. 可能出現「Play 保護機制」的警告，因為這個 App 不是從商店來的。
   點 **仍要安裝**（或「詳細資訊」→「仍要安裝」）

> 找不到網址也沒關係：儲存庫首頁右邊有一欄 **Releases**，點進去最新那筆，
> 下方 **Assets** 裡的 `narrator.apk` 就是。

安裝完成後，請接著看 **`2. 安裝說明.md`**，裡面有安裝後必做的設定。

---

## 之後要更新語音怎麼辦

重新合成問題句、跑過 `tools/build_assets.py` 之後，你會得到新的
`voices.bin` 和 `index.json` 兩個檔案：

1. 在 GitHub 儲存庫點進 `app` → `src` → `main` → `assets`
2. **Add file** → **Upload files**，把新的兩個檔案拖進去（同名會自動覆蓋）
3. **Commit changes**
4. 它會自己重新建置，幾分鐘後同一個網址就是新版本

---

## 如果建置失敗（紅叉）

1. 點 **Actions** → 點那一列紅叉的紀錄
2. 點左邊那個有紅叉的步驟名稱，右邊會展開一長串文字
3. 把最下面那段紅色的錯誤文字**整段複製**，貼給我

這份專案沒有經過實機編譯（我這邊的環境連不上 Google 的伺服器，下載不到
Android SDK），所以第一次建置有可能會卡在某個小地方。錯誤訊息拿給我，
我改完你再重新上傳一次就行——這是預期中的流程，不是出了什麼大問題。
