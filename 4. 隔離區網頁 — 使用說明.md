# 隔離區網頁 — 使用說明

聽到有問題的句子，在手機上標記；之後用這個網頁把它們從抽選池裡拿掉。

音訊**不會被刪除**，只是不再被抽到。改變主意隨時可以放回去。

---

## 〇、先搞清楚會動到什麼

| | |
|---|---|
| 會改的檔案 | 只有 `index.json`（120 KB） |
| 不會動的 | `voices.bin`（19 MB 的語音）、你電腦上的 wav、任何程式碼 |
| 網頁跑在哪 | 你的瀏覽器裡。沒有伺服器、沒有帳號、資料不會傳出去 |

---

## 一、開工前：先把 App 更新到 v1.3

**這一步不能跳。** GitHub 上的版本還沒有隔離功能，舊版看不懂「隔離」這件事——
你就算傳了隔離過的 `index.json` 上去，它也會照播不誤（不會當掉，但隔離不會生效）。

電腦上的 `narrator-apk` 資料夾已經是 v1.3 了（隔離功能＋鎖定畫面修正），重新上傳一次就好：

1. 檔案總管上方 → **檢視** → **顯示** → 確認 **隱藏的項目** 有打勾
   （沒打勾的話 `.github` 不會被選到，整件事不會發生）
2. 打開 `narrator-apk` 資料夾，**Ctrl + A** 全選
3. 到 GitHub 的 `music-ducking` 儲存庫 → **Add file** → **Upload files**
4. 把選好的東西拖進虛線框，等進度條跑完（語音檔 19 MB，會花幾分鐘）
5. 捲到最下面按綠色的 **Commit changes**
6. 點上方 **Actions**，等黃點變成綠勾（3～6 分鐘）
7. 手機開 `https://github.com/cm745633/music-ducking/releases/latest/download/narrator.apk`
   下載安裝，蓋掉舊版

裝好之後，App 的 **設定與匯出** 頁面最下面應該會顯示 **v1.3**。
看到 v1.3 才代表這一步成功了。

> 這次上傳會順便把下一節要用的 `index.html` 和 `.nojekyll` 一起帶上去，
> 它們已經放在 `narrator-apk` 資料夾裡了。

---

## 二、打開 GitHub Pages（只要做這一次）

1. 在 `music-ducking` 儲存庫頁面上方點 **Settings**
2. 左側選單找到 **Pages**
3. **Source** 選 **Deploy from a branch**
4. 下面兩個下拉選單：左邊選 **main**，右邊選 **/ (root)**
5. 按 **Save**
6. 等 1～2 分鐘，同一頁上方會出現一句
   「Your site is live at https://cm745633.github.io/music-ducking/」

**這就是你的網址，把它加到書籤：**

```
https://cm745633.github.io/music-ducking/
```

手機、電腦都能開。之後每次要處理標記，直接開這個網址就好。

---

## 三、從手機匯出標記

1. App → **設定與匯出** → **匯出標記結果到 Download**
2. 手機的 **Download** 資料夾裡會多出三個檔案，我們只要 **`flags.csv`**
3. 用你習慣的方式把 `flags.csv` 弄到電腦上（傳 LINE 給自己、插 USB 線、雲端硬碟都行）

> 也可以直接在手機上做完整套流程——網頁在手機瀏覽器上一樣能用，
> `flags.csv` 就在手機的 Download 裡。只是最後上傳回 GitHub 在電腦上比較好操作。

---

## 四、用網頁勾選

打開網址，畫面會自己載入現有的 600 條索引，最上面顯示
「語音總數 600 / 隔離 0 / 可抽選 600」。

1. 把 `flags.csv` 拖到 **「把 flags.csv 拖進來」** 那一塊
   （手機的話點一下，從檔案選）
2. 下面會列出所有被標記過的句子。四個分頁：

   | 分頁 | 是什麼 |
   |---|---|
   | **新標記待決定** | 這次新標記的，等你決定要不要隔離 |
   | **先前已隔離** | 之前隔離過的，取消打勾就能放回抽選池 |
   | **正面標記** | 你標成 `good` 的。**不提供隔離**，那是你之後補句的參考樣本 |
   | **全部** | 上面三種一起看 |

3. **每條都可以先按 ▶ 試聽再決定。** 這很重要——
   有些標記的延遲很久（會顯示黃色的「延遲 620 秒，可能標錯對象」），
   延遲越久越可能標到的不是你以為的那一句
4. 決定要拿掉的就打勾。最上面的數字會即時變

一些會看到的提示：

- **「同時有 good 標記，請先試聽確認」**（紅色）——你對同一條又按過 good 又按過問題標記，隔離前先聽一次
- **「（未分類）」**——標記了但還沒在 App 裡選 reason，一樣列出來讓你決定
- **「被標記 N 次」**——同一句你標了好幾次，通常代表問題是真的
- 標成 `mistake`（標錯了）的完全不會出現在清單裡

---

## 五、下載，然後傳回 GitHub

1. 按 **「下載 index.json 與 隔離清單.md」**
   （下載前網頁會自己檢查一次，沒過就不給下載，並且把問題印出來給你貼回來）
2. 瀏覽器會下載兩個檔案：

   | 檔案 | 用途 |
   |---|---|
   | `index.json` | 傳回 GitHub，這樣才會生效 |
   | `隔離清單.md` | **留在電腦上。**這是你之後修句時的待辦清單 |

3. 到 GitHub 的 `music-ducking` 儲存庫，依序點進
   **`app` → `src` → `main` → `assets`**
4. **Add file** → **Upload files**，把新的 `index.json` 拖進去（同名會自動覆蓋）
5. 捲到最下面 **Commit changes**
6. 點 **Actions**，等綠勾（3～6 分鐘）
7. 手機重新下載安裝 APK

裝好之後，App 的 **設定與匯出** 頁面最下面應該會顯示
「語音 600 條，其中隔離 N 條，可抽選 M 條」，數字要跟網頁上一致。

被隔離的句子從此不會再被抽到，試播也抽不到，
但**播放歷史、時間軸裡還是看得到原文**——這是刻意的，不然歷史紀錄會變成一串看不懂的代號。

---

## 六、之後要修句的時候

`隔離清單.md` 裡每一條長這樣：

```
### kohaku_007

- 原文：街灯の輪が、雨の上がったばかりの路面に薄く広がっていた。
- 問題：accent — 語調不自然
- 備註：語尾上揚得很怪
- 原始 wav：E:\AivisSpeech Sound\コハク\out_コハク\コハク_007.wav
- 標記日期：2026-08-11
- 隔離日期：2026-08-14
```

那個 wav 路徑可以直接貼到檔案總管的網址列。修句流程：

1. 在 AivisSpeech 裡重新合成，覆蓋原本那個 wav
   （如果是句子本身要改，同時改 `コハク\synth_コハク.py` 裡 `LINES` 陣列的對應那一句）
2. 跑 `python3 tools/build_assets.py --src "E:\AivisSpeech Sound" --out app/src/main/assets`
   產出新的 `voices.bin` 和 `index.json`
3. **重要：這一步會產出全新的 `index.json`，隔離標記會不見。**
   所以要回到隔離區網頁，把新的 `index.json` 拖進去，重新勾選還沒修好的那幾條
4. 兩個檔案都傳回 GitHub

修好的那條記得回網頁到 **先前已隔離** 分頁把打勾取消，它就復活了。

---

## 七、不想開 Pages，或想在沒網路的電腦上用

同一個 `index.html` 檔雙擊就能開，只是要自己餵資料：

1. 雙擊 `narrator-apk\index.html`
2. 網頁會說抓不到索引，出現兩個拖放區
3. 把 `narrator-apk\app\src\main\assets\index.json` 拖進第一個
4. 想試聽的話，把同一個資料夾裡的 `voices.bin` 拖進第二個（不想試聽就跳過）
5. 之後一模一樣

功能完全相同，包括試聽。差別只是每次都要自己拖檔案。

---

## 八、常見問題

**網頁一片空白，或說「沒辦法自動抓取索引」**
Pages 還在部署（第一次要 1～2 分鐘），或是 `index.html` 沒上傳到儲存庫**根目錄**。
按 Ctrl + F5 重新整理一次。真的不行就用第七節的本機做法。

**試聽按鈕是灰的**
沒抓到 `voices.bin`。開網址用的話等一下再重新整理；本機用的話把 `voices.bin` 拖進去。

**按了下載卻只出現一個檔案**
瀏覽器把第二個檔案當成「多重下載」擋掉了。網址列右邊會有一個小圖示問你要不要允許，
點 **允許**。或者按一次「下載」拿到 `index.json`，重新整理後再按一次拿 `隔離清單.md`。

**手機下載的 `隔離清單.md` 打不開**
它就是純文字檔，用任何記事本 App 都能開。你也可以先不管它，之後在電腦上重做一次下載。

**傳了 `index.json` 上去，App 卻沒變**
三個可能：APK 還沒重新下載安裝；裝的還是舊版（設定頁看版本號，要是 v1.3）；
或是傳錯資料夾（要在 `app/src/main/assets/`，不是儲存庫根目錄）。

**我想確認網頁沒有把檔案改壞**
按最上面的 **自我檢查**。它會驗證筆數、offset 連續性、總長度跟 `voices.bin` 是否相符、
每個欄位是不是逐字沒動，還會實際切三段音訊出來確認是完整的 mp3。
有任何一項不對就會用紅字列出來——把紅字貼給我。

---

## 九、技術附錄

寫給下一個接手的人，你可以不用看。

**架構**：單一 `index.html`，無建置步驟、無外部相依、無後端。放在 repo 根目錄，
靠同源相對路徑抓 `./app/src/main/assets/index.json` 與 `voices.bin`，所以沒有 CORS 問題。
根目錄的 `.nojekyll` 是必要的，避免 GitHub Pages 跑 Jekyll。

**輸出格式**：`JSON.stringify(data, null, 1)`。已驗證這個呼叫能把現有的 `index.json`
**逐位元組重建**（121,787 bytes 完全相同），所以 GitHub 上的 diff 只會出現真正改動的行。
欄位順序沿用輸入物件的 key 順序，`quarantined` 附加在每筆最後。

**quarantined 的寫法**：只有 `true` 時才寫入這個欄位；解除隔離時，若輸入檔本來就有這個 key
就寫成 `false`，本來沒有就整個拿掉。這樣第一次使用的 diff 只有 N 行，不是 600 行。

**試聽**：偵測伺服器是否支援 HTTP Range。GitHub Pages 支援，所以每次試聽只抓
`offset`～`offset+length` 那幾十 KB。不支援就退回整份下載後在記憶體切片；
本機模式走 `File.slice()`，不會把 19 MB 讀進記憶體。

**踩過的坑**：交接日誌說切出來的片段開頭應該是 `FF FB`/`FF F3`/`FF FA`，
**實際上不是**——每段 mp3 前面都有一個 45 位元組的 ID3v2 標頭，
要先讀 syncsafe 長度跳過才會看到 frame sync。上一輪是用 ffprobe 驗證的（ffprobe 會自動跳過），
所以沒發現。「自我檢查」裡已經正確處理。

**已驗證的部分**：用 Playwright 實際開啟網頁跑過 116 項斷言，涵蓋
Range / 整份下載 / 本機 `file://` 三種模式、手機與桌機尺寸、
BOM 剝除、CSV 引號跳脫、`mistake` 排除、`good` 鎖定、未分類、延遲標黃、
往返載入、解除隔離、輸出不變量（600 筆、offset 連續、總長 19,741,887、欄位逐字相同）、
音訊實際解碼播放、以及壞掉的輸入。測試用的假 `flags.csv` 是刻意刁鑽版本
（同一條多次標記、先 mistake 後有問題、good 與問題併存、孤兒 clip id）。

**沒有驗證的部分**：使用者的真實 `flags.csv`（他到目前為止還沒正式標記過任何句子）。
測試資料是照 `Exporter.kt` 的 `buildCsv` 逐欄位仿造的。

**刻意沒做**：File System Access API（只有桌面版 Chrome/Edge 支援，權限流程對非開發者是多餘負擔）、
`stats.md` 顯示（使用者明確說不要）、任何形式的音訊處理或重新合成。
