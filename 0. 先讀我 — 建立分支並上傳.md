# 這個資料夾要傳到「分支」，不是傳到 main

正式版住在 `main` 分支。這個診斷版要住在另一個叫 `raw-wav` 的分支，
兩邊各自建置、各自出 APK，互不干擾。

做完之後你手機上會有**兩個 App**：正式版和診斷版，可以並存。

---

## 步驟 1：先把正式版 v1.3 傳上去（隔離功能＋鎖定畫面修正）

這件事跟診斷版無關，但一起做比較省事。

到 `E:\AivisSpeech Sound\narrator-apk\`（**注意是沒有 -raw 的那個**），
照舊：**Add file → Upload files → 拖 `.github`、`app`、`tools` 三個資料夾 + 三個 .md → Commit**。

確認網頁上方的分支選單顯示的是 **main**。

---

## 步驟 2：建立 raw-wav 分支

1. 回到 repo 首頁
2. 點左上角那顆寫著 **main** 的按鈕（旁邊有個樹枝圖示）
3. 在跳出來的輸入框打 `raw-wav`
4. 下面會出現一行 **Create branch: raw-wav from main**，點它

現在那顆按鈕應該變成顯示 **raw-wav**。**接下來每一步都要確認它顯示的是 raw-wav。**

---

## 步驟 3：刪掉分支上用不到的舊語音檔

`raw-wav` 是從 main 複製出來的，所以裡面還有正式版那個 19 MB 的 `voices.bin`。
診斷版不會用到它，留著只會讓 APK 白白胖 19 MB。

1. 確認分支選單顯示 **raw-wav**
2. 依序點進 `app` → `src` → `main` → `assets`
3. 點 `voices.bin`
4. 右上角有排圖示，點 **垃圾桶**（Delete this file）
5. 捲到底，確認寫的是 **Commit directly to the `raw-wav` branch**，按 **Commit changes**

> 如果這裡顯示的是 main，**先停下來**，回步驟 2 重新切分支。

---

## 步驟 4：上傳診斷版的檔案

1. 確認分支選單顯示 **raw-wav**
2. **Add file → Upload files**
3. 到 `E:\AivisSpeech Sound\narrator-apk-raw\`，選這些拖進去：

   ```
   .github     ← 記得要先開「顯示隱藏的項目」
   app
   tools
   （兩個 .md 說明檔，要不要傳都可以）
   ```

4. 檔案有 30 MB，等進度條跑完
5. 捲到底，確認是 **Commit directly to the `raw-wav` branch**，按 **Commit changes**

---

## 步驟 5：下載診斷版

1. 點 **Actions**，會看到一列「建置診斷版 APK」在跑
2. 綠勾之後到 **Releases**，找標籤是 **`diag-數字`** 的那一筆
3. 下載裡面的 `narrator-diag.apk`，手機安裝

> 診斷版**不會**變成 Releases 的 latest，所以正式版原本那個固定下載網址
> （`releases/latest/download/narrator.apk`）還是指向正式版，不受影響。

裝好之後打開「旁白插播 · 診斷」，接著看 `診斷版說明.md`。

---

## 之後要改正式版

切回 **main** 分支再上傳就好。兩個分支互不影響。
